<div class=supreme-container>
<div class="signin">
  <div class="container">
    <div class="row">
      <diw class="col-xs-12 text-center">
        <p>You need to be sign up with Facebook to see this page</p>
        <button type="button" class="navbar-wagon-button1" data-toggle="modal" data-target="#modalsignup" id="signinbutton">Connect with Facebook</button>
      </diw>
    </div>
  </div>
</div>
</div>


<%#=simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <!-- <div class="form-inputs"> -->
    <%#= f.input :email, required: false, autofocus: true %>
    <%#= f.input :password, required: false %>
    <%#= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
  <!-- </div> -->

<!--   <div class="form-actions">
    <%#= f.button :submit, "Log in" %>
  </div>
<%# end %>
 -->

<%= render 'shared/modalsignup' %>
